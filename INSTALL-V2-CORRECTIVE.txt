WILLOW LILY — V2 CORRECTIVE PATCH

This patch is specifically for AFTER the first Connected Sales Flow patch.

REPLACE:
1. live-sales-demo.js
2. venue-demo-live.js

DO NOT replace script.js.
DO NOT replace the dashboard HTML/design.
DO NOT merge to main yet.

WHAT V2 FIXES

1. ONE SESSION RECORD
The live sales workflow now uses the same sessionStorage key as the existing
Willow Lily runtime:
  willow-lily-visit-v2

This lets the existing Venue Intelligence dashboard, tour, proposal, Stripe
success page and couple planning view read one browser-session record.

2. NO DUPLICATE DASHBOARD CARD
venue-demo-live.js no longer injects a second lead card.
The beautiful existing dashboard remains the single dashboard owner.

3. LIVE TOUR -> EXISTING DASHBOARD COMPATIBILITY
A successful live tour request now also writes a lead in the shape that the
existing dashboard already understands.

4. PROPOSAL + PAYMENT STATUS
The existing dashboard receives a small "Live sales workflow" status inside the
existing lead card instead of creating a parallel dashboard.

5. COUPLE VIEW CONTINUITY
The couple page reads tour, proposal and payment from the unified session.

6. DATE CONTINUITY
The live workflow resolves the wedding date from:
availability.finalDate -> acceptedAlternative -> selectedDate -> originalDate.
Run the entire test on ONE Vercel origin so localStorage/sessionStorage remain
available.

7. STALE COPY
Where live-sales-demo.js is loaded, the footer is corrected so it no longer
claims that no messages or payments are connected.
The Venue Intelligence integration note is also corrected:
- Resend demo notification: connected
- Stripe TEST Checkout: connected
- no real payment
- calendar / CRM / authentication / real customer records: simulated

TEST IN THIS ORDER, ALL ON THE SAME PREVIEW DOMAIN:

1. /build
2. /availability
3. /visit
4. submit tour
5. /proposal
6. accept proposal
7. pay Stripe TEST deposit
8. Stripe returns to /deposit-success
9. /couple-demo
10. /venue-demo

EXPECTED:
- Wedding date persists.
- Tour persists.
- Proposal = Accepted.
- Deposit = Test deposit completed.
- Existing Venue Intelligence dashboard shows the local lead.
- No duplicate lead dashboard section appears.
- Existing dashboard visual design is preserved.
